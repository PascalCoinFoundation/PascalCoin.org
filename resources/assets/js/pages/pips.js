const $ = require('jquery');

/**
 * Page handler for the pips listing page.
 */
class Pips
{
    /**
     * Main element.
     *
     * @param {jQuery} $el
     */
    constructor($el)
    {
        this.$el = $el;
    }
}

module.exports = Pips;