const $ = require('jquery');

/**
 * Page handler for the pip detail page.
 */
class Press
{
    /**
     * Main element.
     *
     * @param {jQuery} $el
     */
    constructor($el)
    {
        this.$el = $el;
    }
}

module.exports = Press;